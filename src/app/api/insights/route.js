import { NextResponse } from "next/server";
import { getOrderSummary, listOrders } from "@/lib/db";
import { getAuthenticatedClient } from "@/lib/supabase/server";
export const runtime = "nodejs";
export const maxDuration = 45;
export async function GET() { try { const { supabase, user } = await getAuthenticatedClient(); if (!user) return NextResponse.json({ error: "Debes iniciar sesión." }, { status: 401 }); const { data, error } = await supabase.from("conversations").select("id,question,answer,created_at").eq("user_id", user.id).order("created_at", { ascending: false }).limit(20); if (error) throw error; return NextResponse.json({ conversations: data || [] }); } catch (error) { console.error(error); return NextResponse.json({ error: "No se pudo cargar el historial del asistente." }, { status: 500 }); } }
export async function POST(request) { try { const { question } = await request.json(); if (!question?.trim()) return NextResponse.json({ error: "Escribe una pregunta." }, { status: 400 }); const { supabase, user } = await getAuthenticatedClient(); if (!user) return NextResponse.json({ error: "Debes iniciar sesión." }, { status: 401 }); const [orders, summary] = await Promise.all([listOrders({ supabase, userId: user.id }), getOrderSummary({ supabase, userId: user.id })]); const context = JSON.stringify({ summary, orders: orders.slice(0, 80) }); let answer; if (!process.env.OLLAMA_API_KEY) answer = `Hay ${summary.total} pedidos registrados: ${summary.pending} pendientes y ${summary.received} recepcionados. ${summary.suppliers.length ? `El proveedor con más actividad es ${summary.suppliers[0].supplier}, con ${summary.suppliers[0].total} pedido${summary.suppliers[0].total > 1 ? "s" : ""}.` : "Aún no hay proveedores en el registro."} Configura OLLAMA_API_KEY para activar el análisis profundo con gpt-oss:120b-cloud.`; else { const response = await fetch("https://ollama.com/api/chat", { method: "POST", headers: { "Content-Type": "application/json", Authorization: `Bearer ${process.env.OLLAMA_API_KEY}` }, body: JSON.stringify({ model: "gpt-oss:120b-cloud", stream: false, messages: [{ role: "system", content: "Eres Numa, un analista de compras. Responde en español, con claridad y máximo 120 palabras. Usa solo el contexto entregado; no inventes datos." }, { role: "user", content: `Contexto de pedidos del usuario autenticado:\n${context}\n\nPregunta: ${question.trim()}` }] }) }); const result = await response.json(); if (!response.ok) return NextResponse.json({ error: "Ollama no pudo responder a la consulta." }, { status: 502 }); answer = result.message?.content || "No encontré una respuesta para esa consulta."; } const { error } = await supabase.from("conversations").insert({ user_id: user.id, question: question.trim(), answer }); if (error) throw error; return NextResponse.json({ answer }); } catch (error) { console.error(error); return NextResponse.json({ error: "No se pudo generar el resumen." }, { status: 500 }); } }
