import { NextResponse } from "next/server";
import { createOrder, getOrderSummary, listOrders } from "@/lib/db";
import { getAuthenticatedClient } from "@/lib/supabase/server";
export const runtime = "nodejs";
export const dynamic = "force-dynamic";
export async function GET() { try { const { supabase, user } = await getAuthenticatedClient(); if (!user) return NextResponse.json({ error: "Debes iniciar sesión." }, { status: 401 }); const { data: profile } = await supabase.from("profiles").select("role").eq("id", user.id).single(); const includeAll = profile?.role === "admin"; const [orders, summary] = await Promise.all([listOrders({ supabase, userId: user.id, includeAll }), getOrderSummary({ supabase, userId: user.id, includeAll })]); return NextResponse.json({ orders, summary }); } catch (error) { console.error(error); return NextResponse.json({ error: "No se pudieron cargar los pedidos." }, { status: 500 }); } }
export async function POST(request) { try { const body = await request.json(); const supplier = String(body.supplier || "").trim(); const product = String(body.product || "").trim(); const quantity = Number(body.quantity); const unit = String(body.unit || "unidad").trim(); const requestedDate = String(body.requestedDate || new Date().toISOString().slice(0, 10)); const items = Array.isArray(body.items) ? body.items.map((item) => ({ product: String(item.product || "").trim(), quantity: Number(item.quantity), unit: String(item.unit || "unidades").trim(), unitPrice: Math.max(0, Number(item.unitPrice) || 0) })).filter((item) => item.product && Number.isFinite(item.quantity) && item.quantity > 0) : []; if (!supplier || (!product && !items.length) || (!items.length && (!Number.isFinite(quantity) || quantity <= 0)) || (items.length && items.some((item) => item.quantity <= 0))) return NextResponse.json({ error: "Proveedor y al menos un producto con cantidad válida son obligatorios." }, { status: 400 }); const { supabase, user } = await getAuthenticatedClient(); if (!user) return NextResponse.json({ error: "Debes iniciar sesión." }, { status: 401 }); const id = await createOrder({ supabase, userId: user.id, supplier, product, quantity, unit, items, unitPrice: Math.max(0, Number(body.unitPrice) || 0), priority: ["normal", "high", "urgent"].includes(body.priority) ? body.priority : "normal", notes: String(body.notes || "").trim().slice(0, 500), requestedDate }); return NextResponse.json({ id }, { status: 201 }); } catch (error) { console.error(error); return NextResponse.json({ error: "No se pudo registrar el pedido." }, { status: 500 }); } }
