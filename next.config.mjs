/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow the active development tunnel to load Next.js HMR and font resources.
  allowedDevOrigins: ["antirust-chunk-reverend.ngrok-free.dev"],
};

export default nextConfig;
